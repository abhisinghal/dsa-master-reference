# Mobile audit notes

- Verified interactive animation components already constrain outer SVGs with `width: 100%; height: auto;`, so they should scale within narrow viewports.
- Updated `JavaRunner.vue` mobile styles for stacked controls, a shorter editor, smaller type, tighter padding, and breakable failing-test code output.
- Added mocked landing screenshots that render as responsive images in the home-page screenshot strip.
